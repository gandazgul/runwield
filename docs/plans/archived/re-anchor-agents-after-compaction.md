---
planId: "c62f94f9-b153-4d9f-a814-d0a9497f6d22"
classification: "PLANNED_CHANGE"
workKind: "FEATURE"
complexity: "MEDIUM"
summary: "Add a RunWield extension that re-anchors the active agent on its durable artifact after compaction, so Planner rereads its draft and execution agents reread the Plan and Verification Plan."
affectedPaths:
    - "src/extensions/re-anchor/index.ts"
    - "src/extensions/re-anchor/index.test.ts"
    - "src/shared/session/session.js"
    - "src/shared/workflow/workflow.js"
    - "src/shared/workflow/planning-agent.ts"
    - "src/shared/workflow/workflow-prompts.js"
    - "src/shared/workflow/workflow-prompts.test.js"
    - "src/shared/workflow/workflow.test.js"
    - "src/shared/workflow/epic-continuation.ts"
    - "src/cmd/load-plan/index.ts"
    - "src/cmd/load-plan/index.integration.test.ts"
    - "src/cmd/plans/pull.ts"
    - "src/agent-definitions/architect.md"
    - "src/agent-definitions/shared-practice/plan-execution.md"
    - "src/agent-definitions/subagent-definitions/reviewer-feedback-engineer.md"
objectiveChecks:
    - id: "OC1"
      command: "test -f src/extensions/re-anchor/index.ts"
      rationale: "The re-anchor extension must exist as a TypeScript production module."
    - id: "OC2"
      command: "grep -q \"session_compact\" src/extensions/re-anchor/index.ts && grep -q \"re-anchor\" src/shared/session/session.js"
      rationale: "The extension must listen for compaction and be registered; an unregistered extension cannot re-anchor agents."
    - id: "OC3"
      command: "grep -q \"buildReAnchorMessage\" src/shared/workflow/workflow-prompts.js"
      rationale: "The re-anchor text must be generated by a shared workflow prompt builder rather than inline extension text."
    - id: "OC4"
      command: "grep -q \"setWorkflowPlanName\" src/shared/workflow/workflow.js && deno eval 'const s=await Deno.readTextFile(\"src/cmd/load-plan/index.ts\"); Deno.exit(/runPlanningAgent\\(\\{[^}]{0,500}planName:\\s*plan\\.planName/.test(s)?0:1)'"
      rationale: "Resumed Plans must record their durable Plan pointer before Planner or Architect can compact."
    - id: "OC5"
      command: "deno run -A scripts/run-tests.js -A --no-check src/extensions/re-anchor/index.test.ts"
      rationale: "The new test must exercise retry skip, single injection, artifact resolution, and exclusions."
executionAgent: "engineer"
collaborationRecommendation: "autonomous"
createdAt: "2026-08-01T01:18:18-04:00"
status: "user_verified"
origin: "internal"
userVerifiedAt: "2026-08-07T03:47:41.532Z"
userVerificationNote: "Git has a delivery commit: 36c88963 Re-anchor agents after context compaction, which added the extension and changed the affected workflow files."
workRecord:
    status: "generated"
    recordId: "d1acbedf-69be-4807-b87f-2768ef25d209"
    path: "docs/work-records/2026-08-07-re-anchor-agents-after-compaction.md"
    lastAttemptAt: "2026-08-07T03:47:41.660Z"
updatedAt: "2026-08-09T04:50:11.605Z"
archivedAt: "2026-08-09T04:50:11.605Z"
archivedFromStatus: "user_verified"
archivedFromPath: "docs/plans/re-anchor-agents-after-compaction.md"
---

# Re-Anchor Agents After Compaction

## Context

Long agent turns drift from their durable artifact, and compaction is where the drift becomes invisible. Two observed
failures share the shape:

- A Planner session that settled decisions conversationally can cross a compaction boundary and finalize a Plan that
  omits one, because the only authoritative copy was in the context window.
- All three blocking findings in a validation second pass were things the Plan states explicitly and the engineer lost
  while deep in code. The seam check that failed was in the Verification Plan and never ran.

Both prompts now say to reread the artifact — `planner.md` under **Revising an Existing Plan**, and the engineer prompts
under their verification steps. Neither has a trigger, and an instruction to reread after compaction is read by the very
context compaction just discarded.

RunWield already runs three pi extensions — `mnemoteca`, `cymbal`, and `snip` — registered through `extensionFactories`
at `session.js:1833`. pi's extension API exposes exactly the two events this needs, typed: `session_compact` (fired
after compaction, carrying `reason` and `willRetry`) and `context` (fired before each provider request, and able to
return a replacement `messages` array).

The current pi 0.82.1 type surface does **not** let `session_before_compact` return `customInstructions` for ordinary
compaction; `SessionBeforeCompactResult` only supports `cancel` and a complete replacement `compaction`. The re-anchor
therefore cannot rely on summarizer guidance as a correctness mechanism in this change.

## Objective

After compaction, the active agent receives one message naming the artifact it must reread before continuing: draft Plan
for Planner, Plan plus Verification Plan for Engineer and Frontend Engineer, Plan plus open Review Issue Ledger for
Reviewer-Feedback Engineer, and Epic for Architect.

Non-goal: forcing compaction earlier. Compaction is lossy, and the value is the re-anchor, not the compaction.

## Approach

A new `re-anchor` extension holds one flag. `session_compact` sets it, unless `willRetry` is true — an overflow-retried
compaction fires the event more than once for a single boundary. The next `context` event appends the re-anchor message
to the outgoing messages and clears the flag.

**An extension is the right seam, and the reason is not that RunWield already has three.** Compaction fires _mid-turn_
on `threshold` and `overflow`, so the next provider request after it is usually not a RunWield-built request at all — it
is the same turn continuing. Injecting at RunWield's own turn boundaries (`decoratedRequest`, `buildEngineerRequest`)
would therefore miss the common case entirely and land the re-anchor only on manual `/compact`. The `context` event is
the one seam that fires on the continuation request, which is precisely where the anchor has to land.

The extension factory closes over what it needs. `snipExtension` already establishes the pattern at `session.js:1835`
(`(pi) => snipExtension(pi)`), and `agentName`, `sessionCwd`, and `hostedSession` are in scope at that point. The
extension should read the Plan name from `hostedSession.getActiveExecutionWorkflow()` first for execution/repair turns,
then from `hostedSession.getWorkflowContext()` for planning turns resumed from `load-plan` or after `plan_written`.

The Plan-name source needs one small reinforcement: `runPlanningAgent` should accept an optional `planName` and record
it through `hostedSession.setWorkflowPlanName(planName)` before the Planner or Architect turn starts. `load-plan` and
other Plan re-review/resume callers already know the Plan name at that boundary; recording it there makes the draft Plan
path available after compaction without parsing the lossy transcript.

The message is context, not instruction duplication: it names the file and the sections, and leaves the reason to the
agent prompt, so the two cannot drift the way `buildEngineerRequest` drifted from `engineer.md`.

## Files to Modify

- `src/extensions/re-anchor/index.ts` — new: the extension, its flag, artifact resolution, and both handlers.
  TypeScript, since a new production `.js` under `src/` fails `deno task language-policy:check`.
- `src/extensions/re-anchor/index.test.ts` — new unit coverage for compaction events, context injection, artifact
  resolution, exclusions, and fail-open handling.
- `src/shared/session/session.js` — register the factory alongside the existing three, closing over `agentName`,
  `sessionCwd`, and `hostedSession`.
- `src/shared/workflow/workflow.js` — let `runPlanningAgent` accept and persist an optional `planName` before Planner or
  Architect starts.
- `src/shared/workflow/workflow-prompts.js` — `buildReAnchorMessage`, so agent-facing text stays where the rest lives.
- `src/shared/workflow/workflow-prompts.test.js` — builder coverage for planning, execution, repair, unsupported-agent,
  and missing-artifact messages.
- `src/shared/workflow/workflow.test.js` — coverage that `runPlanningAgent({ planName })` records workflow Plan context
  before the agent turn.
- `src/cmd/load-plan/index.js` — pass `plan.planName` into Planner/Architect resume and re-review calls so draft Plans
  reopened through `load-plan` are re-anchorable.
- `src/cmd/load-plan/load-plan-review.test.js`, `src/cmd/load-plan/load-plan-epic.test.js` — coverage that the concrete
  `load-plan` Planner and Architect paths pass `planName` into `runPlanningAgent`.
- `src/agent-definitions/planner.md`, `engineer.md`, `frontend-engineer.md` — reconcile the existing reread wording so
  prompts state the expectation while the injected message supplies only path and sections.

## Reuse Opportunities

- `src/extensions/mnemoteca/index.js` — the extension shape: default-export factory taking `pi`,
  `pi.on("session_start")` to capture cwd and project identity, state held in factory closure.
- `src/extensions/*/index.test.js` — the established pattern for testing an extension by driving its handlers directly.
- `src/shared/workflow/workflow-prompts.js` — `buildEngineerRequest` and `buildTriageReport` as the builder pattern.
- `src/shared/session/workflow-context-session.js` — normalized Plan-name storage already used for footer/workflow
  context.
- `src/shared/workflow/review-ledger.ts` — `renderOpenItems` for injecting the open Review Issue Ledger into repair
  re-anchor messages without inventing a second ledger format.

## Implementation Steps

- [ ] `src/extensions/re-anchor/index.ts` default-exports a factory registering `session_compact` and `context`
      handlers, and `session.js` pushes it onto `extensionFactories` closing over the active `agentName`, `sessionCwd`,
      and `hostedSession`.
- [ ] A `session_compact` event with `willRetry: true` leaves the flag unset, so one overflow-retry compaction produces
      at most one re-anchor when the settled post-retry boundary fires.
- [ ] The first `context` event after a settled compaction returns a new `messages` array equal to the incoming messages
      plus one user-role re-anchor message; the following `context` event returns the messages unchanged.
- [ ] The extension resolves Plan paths from `hostedSession.getActiveExecutionWorkflow().planName` before
      `hostedSession.getWorkflowContext().planName`, formats them as `plans/<name>.md`, and returns no artifact when no
      normalized Plan name exists.
- [ ] `runPlanningAgent` records an optional `planName` through `hostedSession.setWorkflowPlanName(planName)` before the
      root Planner/Architect turn; `load-plan` resume and re-review paths pass their known `plan.planName` into that
      option.
- [ ] `buildReAnchorMessage(context)` in `workflow-prompts.js` returns per-agent text naming the artifact path and the
      sections to reread. An agent with no durable artifact, `delegated`, `reviewer`, `slicer`, or another unsupported
      agent returns null and nothing is injected.
- [ ] Reviewer-Feedback Engineer re-anchor text includes the Plan path plus
      `renderOpenItems(activeWorkflow.reviewLedger)` output when open review issues exist; it does not invent or mutate
      ledger state.
- [ ] A Delegated Agent Session receives no re-anchor: a delegated child has a brief, not a Plan.
- [ ] A handler that throws is caught inside the extension and leaves the turn unmodified, so a re-anchor failure cannot
      break a provider request.
- [ ] `src/extensions/re-anchor/index.test.ts` proves the retry skip, single-injection-per-boundary, Plan-name
      precedence, planning-context capture, reviewer-feedback ledger injection, delegated/unsupported-agent exclusion,
      and throwing-handler containment.
- [ ] `planner.md`, `engineer.md`, and `frontend-engineer.md` state the reread expectation once, with the injected
      message supplying only path and sections.

## Verification Plan

- Automated: `deno run -A scripts/run-tests.js -A --no-check src/extensions/re-anchor/index.test.ts`
- Automated: `deno run -A scripts/run-tests.js -A --no-check src/shared/workflow/workflow-prompts.test.js`
- Automated:
  `deno run -A scripts/run-tests.js -A --no-check src/shared/workflow/workflow.test.js src/cmd/load-plan/load-plan-review.test.js src/cmd/load-plan/load-plan-epic.test.js`
  — includes targeted coverage proving resumed/re-review Planner and Architect turns record the known Plan name before
  the agent starts.
- Automated: `deno task ci`.
- Optional automated: `deno task test:golden-tui` if an existing Golden scenario can compact mid-execution cheaply; the
  required assertion is that the next provider request names the Plan path.
- Manual: reopen a draft Plan with `/load-plan`, force `/compact`, then continue; confirm the next Planner/Architect
  request includes `plans/<name>.md` and instructs rereading the artifact.
- Existing behavior to preserve: compaction itself, the `compactionFinished` TUI notification, `/compact`, Session
  Transcript continuity, root Agent switching, `load-plan` resume/re-review behavior, and the three existing extensions.
  The re-anchor adds a message; it must not alter what compaction keeps or discards.
- Behavior expected to stop existing: an agent resuming after compaction with no pointer back to its artifact.

### Objective-Failing Checks

- `OC1` — `test -f src/extensions/re-anchor/index.ts` — the extension exists as TypeScript.
- `OC2` —
  `grep -q "session_compact" src/extensions/re-anchor/index.ts && grep -q "re-anchor" src/shared/session/session.js` —
  it is both written and registered; an unregistered extension never runs.
- `OC3` — `grep -q "buildReAnchorMessage" src/shared/workflow/workflow-prompts.js` — the message is a shared builder,
  not inline text destined to drift from the prompts.
- `OC4` —
  `grep -q "setWorkflowPlanName" src/shared/workflow/workflow.js && deno eval 'const s=await Deno.readTextFile("src/cmd/load-plan/index.js"); Deno.exit(/runPlanningAgent\(\{[^}]{0,500}planName:\s*plan\.planName/.test(s)?0:1)'`
  — resumed Plans record their durable Plan pointer before Planner/Architect can compact.
- `OC5` — `deno run -A scripts/run-tests.js -A --no-check src/extensions/re-anchor/index.test.ts` — the retry skip,
  single-injection behavior, artifact resolution, and exclusions are exercised.

## Execution Policy

- Execute with `executionAgent: engineer` and `collaborationRecommendation: autonomous`; this is runtime/session
  behavior with no browser-visible UI surface.
- No dev server or headed browser verification is required.

## Edge Cases & Considerations

- **The re-anchor must not become a second instruction source.** This is the drift that put "use the edit tool" in code
  while the prompt said something else. The message names an artifact; the prompt owns the behavior.
- The `context` event can replace the entire message array. Append only; returning a rebuilt array risks dropping
  history, and this extension has no business editing anything but its own addition.
- `context` fires on every provider request including sub-agent and tool-continuation requests. Injecting on the wrong
  one puts a Plan pointer into a delegated child's context.
- Compaction during Workflow Validation repair means the active agent is Reviewer-Feedback Engineer, not Engineer. Its
  artifact is the Plan plus the open Review Issue Ledger.
- The extension API is upstream in `pi-mono/packages/coding-agent`. `ContextEventResult`, `SessionCompactEvent`, and the
  lack of `customInstructions` on `SessionBeforeCompactResult` are the shapes this depends on; pin behavior against the
  vendored version.
- Registering a fourth extension touches session construction, which is on the path of every command. The factory and
  handlers must be side-effect-light and fail open for runtime resolution errors; compile/import errors should still be
  caught by `deno task ci` rather than hidden.

[Mnemoteca]: https://github.com/gandazgul/mnemoteca
