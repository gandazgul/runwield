---
import LoadingSurface from "../../../components/LoadingSurface.astro";
import WorkspaceLayout from "../../../layouts/WorkspaceLayout.astro";
import { ReviewDevSurface } from "../../../react/ReviewDevSurface.tsx";

if (!import.meta.env.DEV) {
    return new Response("Not found", { status: 404 });
}
const variant = Astro.url.searchParams.get("variant") || "feature";
const reviewTitle = variant === "project" ? "Fixture PROJECT Epic: Plan Review UI" : "Fixture Test Plan: Plan Review UI";
---
<WorkspaceLayout
    title="Workspace Plan Review Dev · RunWield"
    surfaceTitle={`Plan Review — ${reviewTitle}`}
    selectedTab="projects"
    url={Astro.url.href}
>
    <ReviewDevSurface surface="plan" presentation="workspace" variant={variant} client:only="react">
        <div slot="fallback"><LoadingSurface label="Loading review" /></div>
    </ReviewDevSurface>
</WorkspaceLayout>
