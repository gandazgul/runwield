// Minimal UnoCSS config — uses defaults, no custom rules needed.
// This file exists solely to suppress the "Config file not found" warning.
import { defineConfig } from "unocss";

export default defineConfig({});
